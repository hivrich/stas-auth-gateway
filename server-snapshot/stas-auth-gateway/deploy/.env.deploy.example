# Copy this to deploy/.env.deploy and fill values. Do NOT commit secrets.

# === SSH (root to target host) ===
SSH_HOST='root@109.172.46.200'
SSH_PASS='REDACTED_PASSWORD'

# === Public API Keys (external clients) ===
STAS_API_KEY=''
MCP_API_KEY=''

# === Intervals.icu (Basic auth) ===
INTERVALS_API_BASE_URL='https://intervals.icu/api/v1'
INTERVALS_API_KEY=''
INTERVALS_ATHLETE_ID=''

# === STAS DB ===
DB_HOST=''
DB_PORT='5432'
DB_NAME=''
DB_USER=''
DB_PASSWORD=''
DB_SSL='false'

# === Conventions ===
DEFAULT_USER_ID=''

# === Domain ===
TARGET_DOMAIN='intervals.stas.run'

# === Optional: OAuth client defaults for stas-auth-gateway ===
OAUTH_CLIENT_ID='chatgpt-actions'
OAUTH_REDIRECTS='https://chat.openai.com/aip/*/oauth/callback'

# === Local tools (macOS) ===
# If you use password auth, install sshpass locally:
#   brew install hudochenkov/sshpass/sshpass
# Or switch to SSH keys and omit SSH_PASS
