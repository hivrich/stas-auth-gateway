# Example systemd override for stas-auth-gateway.service
# Place at: /etc/systemd/system/stas-auth-gateway.service.d/override.conf
# Then run: systemctl daemon-reload && systemctl restart stas-auth-gateway

[Service]
# Bind only on loopback behind reverse proxy
Environment=HOST=127.0.0.1
# Example: increase file limits if needed
# LimitNOFILE=65535
