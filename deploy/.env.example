# STAS Auth Gateway .env (example)
# Copy to /opt/stas-auth-gateway/.env on the server and fill real secrets.

PORT=3337
NODE_ENV=production

# Backends
STAS_API_BASE=https://stas.stravatg.ru
STAS_API_KEY=7ca1e3d9d8bb76a1297a9c7d9e39d5eaf4d0d6da249440eea43bb50ff0fddf27
ICU_API_BASE=https://intervals.icu/api/v1

# PostgreSQL (for gw_oauth_clients)
PGHOST=94.241.141.239
PGPORT=5432
PGDATABASE=hivrich_db
PGUSER=limpid_beaker67
PGPASSWORD=jup64918
PGSSL=false

# OAuth (ChatGPT client)
OAUTH_CLIENT_ID=chatgpt-actions
OAUTH_CLIENT_SECRET=<PASTE_NEW_OAUTH_CLIENT_SECRET>
OAUTH_REDIRECTS=https://chat.openai.com/aip/*/oauth/callback

# Crypto / Security
SESSION_SECRET=<random_32+>
JWT_SECRET=<random_32+>
CORS_ORIGINS=*

DEBUG=true

# Token TTLs (seconds)
ACCESS_TTL_SEC=3600
REFRESH_TTL_SEC=2592000
